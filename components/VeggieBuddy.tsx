"use client";

import { StoreBuddy } from "@aurora-studio/starter-core";

/** Thin wrapper — persona comes from store `verticalProfile` in starter-core. */
export function VeggieBuddy() {
  return <StoreBuddy />;
}
