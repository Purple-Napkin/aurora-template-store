/** SSR-passed featured kit for the store home hero (right column). */
export type StoreFeaturedProject = {
  slug: string;
  title: string;
  description: string | null;
  imageUrl: string | null;
};
